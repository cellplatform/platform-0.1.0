{
  "name": "demo.parcel-v1.hello-world",
  "version": "0.0.0",
  "scripts": {
    "start": "parcel src/index.html",
    "watch": "yarn start",
    "bundle": "parcel build src/index.html",
    "clean": "rm -rf dist && rm -rf .cache",
    "serve": "cd dist && serve"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  },
  "private": true
}
