import { equals, uniq, flatten } from 'ramda';
export const R = { equals, uniq, flatten };

export { value, defaultValue } from '@platform/util.value';
export { diff } from '@platform/util.diff';

export { Uri } from '@platform/cell.schema';
