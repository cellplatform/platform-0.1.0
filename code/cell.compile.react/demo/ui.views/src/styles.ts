import { style } from '@platform/react';

style.global({
  body: {
    backgroundColor: '#ECECEC',
    fontFamily: 'sans-serif',
    padding: 25,
  },
  h1: {
    fontSize: 64,
    margin: 0,
  },
});
