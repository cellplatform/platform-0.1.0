import { value, defaultValue } from '@platform/util.value';
export { value, defaultValue };

export * from '@platform/cli.spec';

import { events, containsFocus, Keyboard } from '@platform/react';
export { events, containsFocus, Keyboard };

import { CssValue, css, style, color } from '@platform/css';
export { CssValue, css, style, color };

import { str } from '@platform/util.string';
export { str };

/**
 * [Ramda]
 */
import { equals } from 'ramda';
export const R = { equals };
