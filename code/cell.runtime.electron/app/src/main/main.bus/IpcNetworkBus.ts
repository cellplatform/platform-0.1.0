/**
 * An event-bus distributed across a number of electron windows
 * using the IPC (inter-process communication) transport.
 *
 * Refs:
 *    https://www.electronjs.org/docs/api/ipc-main
 */
export function IpcNetworkBus() {
  //
}
