export * from '../common';
export { ModuleRegistry } from '../../data';

/**
 * Common paths used by the Bundle system.
 */
export const BundlePaths = {
  dir: {
    /**
     * The folder on a cell/filesystem the bundled "distribution" files are stored.
     */
    dist: 'dist',
  },
};
