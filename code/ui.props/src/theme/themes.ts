import { t, COLORS } from '../common';

export const LIGHT: t.IPropsTheme = {
  type: 'LIGHT',
  color: COLORS.DARK,
};

export const DARK: t.IPropsTheme = {
  type: 'DARK',
  color: COLORS.WHITE,
};
