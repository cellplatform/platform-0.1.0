/**
 * NOTE:  Inlined from `react-inspector` because the package
 *        stopped publishing these assets to NPM as part of the module.
 * See:
 *   - https://github.com/xyc/react-inspector#theme
 *
 */

import LIGHT from './theme.light';
import DARK from './theme.dark';

export const THEME = {
  LIGHT,
  DARK,
};
