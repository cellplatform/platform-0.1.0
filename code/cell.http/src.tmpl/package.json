{
  "name": "instance",
  "version": "0.0.101",
  "scripts": {
    "now": "now dev --listen 8080",
    "start": "ts-node -T src/server",
    "watch": "nodemon --watch ../src --watch ../../cell.db/src -e ts,tsx --delay 2"
  },
  "dependencies": {
    "@platform/cell.http": "^0.6.64",
    "@platform/cell.schema": "^0.4.49",
    "@platform/fsdb.mongo": "0.0.99",
    "@platform/fsdb.nedb": "1.3.94"
  },
  "devDependencies": {
    "ts-node": "8.6.2"
  },
  "engines": {
    "node": ">=10.x"
  },
  "private": true
}
