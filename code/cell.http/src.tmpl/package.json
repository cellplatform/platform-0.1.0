{
  "name": "instance",
  "version": "0.0.13",
  "scripts": {
    "now": "now dev --listen 8080",
    "start": "ts-node -T src/server",
    "watch": "nodemon --watch ../src --watch ../../cell.db/src -e ts,tsx --delay 2"
  },
  "dependencies": {
    "@platform/cell.client": "^0.0.2",
    "@platform/cell.fs": "0.1.12",
    "@platform/cell.http": "0.4.25",
    "@platform/cell.schema": "^0.1.14",
    "@platform/fsdb.mongo": "0.0.45",
    "@platform/fsdb.nedb": "1.3.40"
  },
  "devDependencies": {
    "ts-node": "8.5.2"
  },
  "engines": {
    "node": ">=10.x"
  },
  "private": true
}
