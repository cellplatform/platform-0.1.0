{
  "name": "instance",
  "version": "0.0.42",
  "scripts": {
    "now": "now dev --listen 8080",
    "start": "ts-node -T src/server",
    "watch": "nodemon --watch ../src --watch ../../cell.db/src -e ts,tsx --delay 2"
  },
  "dependencies": {
    "@platform/cell.fs": "0.2.2",
    "@platform/cell.http": "0.4.56",
    "@platform/cell.schema": "^0.2.14",
    "@platform/fsdb.mongo": "0.0.60",
    "@platform/fsdb.nedb": "1.3.55"
  },
  "devDependencies": {
    "ts-node": "8.5.4"
  },
  "engines": {
    "node": ">=10.x"
  },
  "private": true
}
