{
  "name": "instance",
  "version": "0.0.150",
  "scripts": {
    "now": "now dev --listen 8080",
    "start": "ts-node -T src/server",
    "watch": "nodemon --watch ../src --watch ../../cell.db/src --watch ../../cell.http.router/src -e ts,tsx --delay 2"
  },
  "dependencies": {
    "@platform/cell.fs.local": "^0.4.64",
    "@platform/cell.fs.s3": "^0.4.67",
    "@platform/cell.http": "^0.7.100",
    "@platform/cell.http.router": "^0.7.98",
    "@platform/cell.schema": "^0.4.120",
    "@platform/fsdb.mongo": "0.0.118",
    "@platform/fsdb.nedb": "1.3.113"
  },
  "devDependencies": {
    "ts-node": "8.10.1"
  },
  "engines": {
    "node": ">=10.x"
  },
  "private": true
}
