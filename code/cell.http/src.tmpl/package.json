{
  "name": "instance",
  "version": "0.0.70",
  "scripts": {
    "now": "now dev --listen 8080",
    "start": "ts-node -T src/server",
    "watch": "nodemon --watch ../src --watch ../../cell.db/src -e ts,tsx --delay 2"
  },
  "dependencies": {
    "@platform/cell.http": "0.6.16",
    "@platform/cell.schema": "^0.4.22",
    "@platform/fsdb.mongo": "0.0.89",
    "@platform/fsdb.nedb": "1.3.84"
  },
  "devDependencies": {
    "ts-node": "8.6.2"
  },
  "engines": {
    "node": ">=10.x"
  },
  "private": true
}
