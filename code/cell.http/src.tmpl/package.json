{
  "name": "instance",
  "version": "0.0.0",
  "scripts": {
    "now": "now dev --listen 8080",
    "start": "ts-node -T src/server",
    "watch": "nodemon --watch ../src --watch ../../cell.db/src -e ts,tsx --delay 2"
  },
  "dependencies": {
    "@platform/cell.fs": "0.3.10",
    "@platform/cell.http": "0.5.14",
    "@platform/cell.schema": "^0.3.8",
    "@platform/fsdb.mongo": "0.0.61",
    "@platform/fsdb.nedb": "1.3.56"
  },
  "devDependencies": {
    "ts-node": "8.5.4"
  },
  "engines": {
    "node": ">=10.x"
  },
  "private": true
}
