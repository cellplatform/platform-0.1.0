{
  "name": "@platform/cell.http.tmpl",
  "version": "0.0.79",
  "scripts": {
    "now": "now dev --listen 8080",
    "deploy": "now --prod --force $@",
    "start": "ts-node -T src/server",
    "watch": "nodemon --watch ../src --watch ../../cell.db/src -e ts,tsx --delay 2"
  },
  "dependencies": {
    "@platform/cell.http": "0.3.0",
    "@platform/fsdb.mongo": "0.0.42",
    "@platform/fsdb.nedb": "1.3.37"
  },
  "devDependencies": {
    "ts-node": "8.5.2"
  },
  "engines": {
    "node": ">=10.x"
  },
  "license": "MIT",
  "private": true
}
