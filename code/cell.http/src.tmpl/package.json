{
  "name": "instance",
  "version": "0.0.33",
  "scripts": {
    "now": "now dev --listen 8080",
    "start": "ts-node -T src/server",
    "watch": "nodemon --watch ../src --watch ../../cell.db/src -e ts,tsx --delay 2"
  },
  "dependencies": {
    "@platform/cell.fs": "0.1.28",
    "@platform/cell.http": "0.4.47",
    "@platform/cell.schema": "^0.2.10",
    "@platform/fsdb.mongo": "0.0.55",
    "@platform/fsdb.nedb": "1.3.50"
  },
  "devDependencies": {
    "ts-node": "8.5.4"
  },
  "engines": {
    "node": ">=10.x"
  },
  "private": true
}
