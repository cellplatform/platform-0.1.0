{
  "name": "instance",
  "version": "0.0.10",
  "scripts": {
    "now": "now dev --listen 8080",
    "start": "ts-node -T src/server",
    "watch": "nodemon --watch ../src --watch ../../cell.db/src -e ts,tsx --delay 2"
  },
  "dependencies": {
    "@platform/cell.fs": "0.0.4",
    "@platform/cell.http": "0.3.16",
    "@platform/fsdb.mongo": "0.0.44",
    "@platform/fsdb.nedb": "1.3.39"
  },
  "devDependencies": {
    "ts-node": "8.5.2"
  },
  "engines": {
    "node": ">=10.x"
  },
  "private": true
}
