{
  "name": "@platform/cell.http.sample",
  "version": "0.0.54",
  "scripts": {
    "now": "now dev --listen 8080",
    "deploy": "now --prod",
    "start": "ts-node -T src/server",
    "watch": "nodemon --watch ../src --watch ../../cell.db/src -e ts,tsx --delay 2"
  },
  "dependencies": {
    "@platform/cell.http": "0.1.22",
    "@platform/fsdb.mongo": "0.0.35",
    "@platform/fsdb.nedb": "1.3.30"
  },
  "devDependencies": {
    "ts-node": "8.5.2"
  },
  "engines": {
    "node": ">=10.x"
  },
  "license": "MIT",
  "private": true
}
