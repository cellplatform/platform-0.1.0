{
  "name": "sample.cell.server",
  "version": "0.0.26",
  "scripts": {
    "now": "now dev --listen 8080",
    "deploy": "now --prod",
    "start": "ts-node -T src/server",
    "watch": "nodemon --watch ../src --watch ../../cell.db/src -e ts,tsx --delay 2"
  },
  "dependencies": {
    "@platform/cell.http": "0.0.26"
  },
  "devDependencies": {
    "ts-node": "8.4.1"
  },
  "engines": {
    "node": ">=10.x"
  },
  "license": "MIT",
  "private": true
}
