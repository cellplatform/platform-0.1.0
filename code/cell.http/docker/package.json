{
  "name": "cell.docker",
  "version": "0.0.6",
  "main": "lib/index",
  "scripts": {
    "start": "node lib/index",
    "build": "ts build --no-esm $@",
    "dbuild": "docker build -t domains/cell .",
    "drun": "docker run --rm -p 8080:8080 domains/cell",
    "dup": "docker-compose up",
    "dup-build": "docker-compose up --build"
  },
  "dependencies": {
    "@platform/cell.fs.local": "^0.4.145",
    "@platform/cell.fs.s3": "^0.4.150",
    "@platform/cell.http": "^0.7.231",
    "@platform/cell.http.router": "^0.8.54",
    "@platform/cell.runtime": "^0.0.10",
    "@platform/cell.schema": "^0.5.23",
    "@platform/cell.types": "^0.5.24",
    "@platform/fsdb.mongo": "^0.0.148",
    "@platform/fsdb.nedb": "^1.3.142"
  },
  "devDependencies": {
    "@platform/ts": "^4.0.6"
  },
  "license": "MIT",
  "private": true
}
