{
  "name": "cell.docker",
  "version": "0.0.11",
  "main": "lib/index",
  "scripts": {
    "start": "node lib/index",
    "build": "ts build --no-esm $@",
    "dbuild": "docker build -t domains/cell .",
    "drun": "docker run --rm -p 8080:8080 domains/cell",
    "dup": "docker-compose up",
    "dup-build": "docker-compose up --build"
  },
  "dependencies": {
    "@platform/cell.fs.local": "^0.4.147",
    "@platform/cell.fs.s3": "^0.4.152",
    "@platform/cell.http": "^0.7.236",
    "@platform/cell.router": "^0.8.59",
    "@platform/cell.runtime": "^0.0.12",
    "@platform/cell.schema": "^0.5.25",
    "@platform/cell.types": "^0.5.26",
    "@platform/fsdb.mongo": "^0.0.148",
    "@platform/fsdb.nedb": "^1.3.142"
  },
  "devDependencies": {
    "@platform/ts": "^4.0.6"
  },
  "license": "MIT",
  "private": true
}
