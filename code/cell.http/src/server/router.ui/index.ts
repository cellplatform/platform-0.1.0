import { t } from '../common';

/**
 * Register routes.
 */
export function init(args: {
  db: t.IDb;
  fs: t.IFileSystem;
  router: t.IRouter;
  title?: string;
  deployedAt?: number;
}) {
  console.log('\n🐷 TODO register UI routes (/router.ui/index.ts)\n');
}
