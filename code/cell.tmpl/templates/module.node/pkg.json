{
  "name": "__NAME__",
  "version": "0.0.0",
  "sideEffects": false,
  "scripts": {
    "start": "ts-node -T src/main/index",
    "dev": "yarn start",
    "test": "ts test $@",
    "lint": "ts lint $@",
    "build": "ts build --no-esm $@",
    "bundle": "cell.compiler bundle node $@",
    "watch": "cell.compiler watch node $@",
    "info": "cell.compiler info node $@",
    "clean": "cell.compiler clean $@"
  },
  "dependencies": {
    "@platform/log": "~latest"
  },
  "devDependencies": {
    "@platform/cell.compiler": "~latest"
  },
  "license": "MIT",
  "private": true
}
