{
  "name": "ui.shell.test",
  "version": "0.1.17",
  "description": "Test module for ui.shell (and associated modules).",
  "scripts": {
    "ssr": "cd src.server && yarn ssr $@",
    "ui": "ui $@",
    "start": "ui start web $@",
    "watch": "cd src.server && yarn watch $@",
    "test": "ts test",
    "tdd": "ts test --watch",
    "lint": "ts lint",
    "build": "ts build --no-esm $@",
    "build-test": "ts build --tsconfig=tsconfig.test $@",
    "prepare": "ts prepare",
    "bundle": "ui bundle web",
    "deploy": "cd src.server && yarn deploy",
    "nuke": "rm -rf node_modules && cd src.server && rm -rf node_modules"
  },
  "dependencies": {
    "@platform/react": "0.3.24",
    "@platform/ui.button": "0.3.4",
    "@platform/ui.datagrid": "0.5.13",
    "@platform/ui.icon": "0.0.88",
    "@platform/ui.loader": "0.2.4",
    "@platform/ui.shell": "0.1.15",
    "@platform/ui.shell.doc": "0.0.11",
    "@platform/ui.shell.types": "0.0.10"
  },
  "devDependencies": {
    "@platform/test": "0.0.63",
    "@platform/ts": "3.5.4",
    "@uiharness/dev": "0.9.3",
    "@uiharness/ui": "0.6.14"
  },
  "files": [
    "lib",
    "!lib/sample",
    "!*.TEST.{js, mjs, d.ts}"
  ],
  "license": "MIT",
  "private": true
}
