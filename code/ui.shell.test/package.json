{
  "name": "ui.shell.test",
  "version": "0.1.3",
  "description": "Test module for ui.shell (and associated modules).",
  "scripts": {
    "ssr": "cd sample.server && yarn ssr $@",
    "ui": "ui $@",
    "start": "ui start web $@",
    "watch": "cd sample.server && yarn watch $@",
    "test": "ts test",
    "tdd": "ts test --watch",
    "lint": "ts lint",
    "build": "ts build --no-esm $@",
    "build-test": "ts build --tsconfig=tsconfig.test $@",
    "prepare": "ts prepare",
    "bundle": "ui bundle web",
    "deploy": "cd sample.server && yarn deploy",
    "nuke": "rm -rf node_modules && cd sample.server && rm -rf node_modules"
  },
  "dependencies": {
    "@platform/react": "0.3.20",
    "@platform/ui.icon": "0.0.84",
    "@platform/ui.shell": "0.1.3"
  },
  "devDependencies": {
    "@platform/test": "0.0.61",
    "@platform/ts": "3.5.1",
    "@uiharness/dev": "0.9.3",
    "@uiharness/ui": "0.6.14"
  },
  "files": [
    "lib",
    "!lib/sample",
    "!*.TEST.{js, mjs, d.ts}"
  ],
  "license": "MIT"
}
