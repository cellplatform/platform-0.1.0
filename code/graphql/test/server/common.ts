import * as express from 'express';

export { express };
export { ApolloServer, gql } from 'apollo-server-micro';
export { log } from '@platform/log/lib/server';
export { is } from '@platform/util.is';
