export { value, defaultValue } from '@platform/util.value';
export { MemoryCache } from '@platform/cache';

/**
 * Ramda.
 */
import {
  ascend,
  clamp,
  descend,
  flatten,
  identity,
  memoizeWith,
  prop,
  sortWith,
  uniq,
  uniqBy,
  pipe,
} from 'ramda';

export const R = {
  ascend,
  clamp,
  descend,
  flatten,
  identity,
  memoizeWith,
  prop,
  sortWith,
  uniq,
  uniqBy,
  pipe,
};
