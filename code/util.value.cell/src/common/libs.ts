export { value, defaultValue } from '@platform/util.value';

/**
 * Ramda.
 */
import {
  ascend,
  clamp,
  descend,
  flatten,
  prop,
  sortWith,
  uniq,
  uniqBy,
  memoizeWith,
  identity,
} from 'ramda';
export const R = {
  ascend,
  clamp,
  descend,
  flatten,
  prop,
  sortWith,
  uniq,
  uniqBy,
  memoizeWith,
  identity,
};
