export { value, defaultValue } from '@platform/util.value';

/**
 * Ramda.
 */
import { ascend, clamp, descend, flatten, prop, sortWith, uniq, uniqBy } from 'ramda';
export const R = { ascend, clamp, descend, flatten, prop, sortWith, uniq, uniqBy };
