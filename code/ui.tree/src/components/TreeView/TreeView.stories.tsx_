import { React, describe } from '../../test/storybook';
import { Test } from './Test';

describe('components', {
  title: 'A hierarchical tree viewer.',
  width: '100%',
  height: '100%',
  hr: false,
  padding: 30,
  flex: true,
}).add('TreeView', () => <Test />);
