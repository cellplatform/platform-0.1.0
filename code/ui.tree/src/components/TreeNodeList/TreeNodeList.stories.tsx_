import { React, describe } from '../../test/storybook';
import { Test } from './Test';

describe('components', {
  title: 'A scrolling list of tree-nodes.',
  width: '100%',
  height: '100%',
  hr: false,
  padding: 30,
  flex: true,
}).add('TreeNodeList', () => <Test />);
