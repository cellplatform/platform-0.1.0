import { JsonEvents as Events } from './Json.Events';
import { JsonController as Controller } from './Json.Controller';

export * from './Json.Events';

export const Json = {
  Events,
  Controller,
};
