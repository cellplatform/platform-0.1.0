import { DocMarkdownBlock as Markdown } from '../Doc.Block.Markdown';
import { DocInsetPanelBlock as InsetPanel } from '../Doc.Block.InsetPanel';

export const DocBlock = {
  Markdown,
  InsetPanel,
};
