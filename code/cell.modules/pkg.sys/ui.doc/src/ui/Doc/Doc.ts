import { toBlockElements } from './Doc.toBlocks';
import { Index, Layout, Headline, Byline, Block, Image } from './libs';

export const Doc = {
  Index,
  Layout,
  Headline,
  Byline,
  Block,
  Image,
  toBlockElements,
};
