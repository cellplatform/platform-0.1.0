import { t } from '../common';

/**
 * Convert a document definition to a list of BLOCK elements.
 */
export function toBlockElements(def: t.DocDef) {
  /**
   * TODO 🐷
   */

  console.log('convert def', def);

  return []; //
}
