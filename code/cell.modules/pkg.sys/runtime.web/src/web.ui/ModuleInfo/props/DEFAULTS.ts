import * as m from '../types';

/**
 * Default fields and order.
 */
export const FIELDS: m.ModuleInfoFields[] = [
  'url',
  'namespace',
  'version',
  'compiled',
  'kind',
  'files',
  'hash.files',
  'remote',
  'remote.exports',
];
