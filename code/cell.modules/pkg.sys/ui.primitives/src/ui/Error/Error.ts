import { ErrorBoundary as Boundary, ErrorViewDefault as Default } from '../Error.Boundary';

/**
 * Index of [Error] related primitives.
 */
export const Error = {
  Boundary,
  View: {
    Default,
  },
};
