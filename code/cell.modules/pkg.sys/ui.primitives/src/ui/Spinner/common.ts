import { t } from '../common';
export * from '../common';

export { Is } from '@platform/util.is';

export const DEFAULT = {
  sizes: <t.SpinnerSizes[]>[12, 18, 22, 32],
  size: <t.SpinnerSizes>22,
};
