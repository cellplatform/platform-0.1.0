import { ListStateMonitor as Monitor } from './ListState.Monitor';
import { ListSelection as Selection } from './Selection';

/**
 * Tools for working with dynamic <List> state.
 */
export const ListState = {
  Monitor,
  Selection,
};
