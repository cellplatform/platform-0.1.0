export { value, defaultValue, id } from '@platform/util.value';
export { MemoryCache } from '@platform/cache';

/**
 * Ramda.
 */
import {
  ascend,
  clamp,
  descend,
  flatten,
  identity,
  memoizeWith,
  prop,
  sortWith,
  uniq,
  uniqBy,
  pipe,
  equals,
} from 'ramda';

export const R = {
  ascend,
  clamp,
  descend,
  flatten,
  identity,
  memoizeWith,
  prop,
  sortWith,
  uniq,
  uniqBy,
  pipe,
  equals,
};
