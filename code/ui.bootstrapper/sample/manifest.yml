sites:
  - name: dev
    domain:
      - localhost
      - /.*\.now\.sh$/
    bundle: bundle/0.0.12
    routes:
      default:
        entry: index.html
        path:
          - /
  - name: prod
    domain:
      - bootstrapper.platform.uiharness.com
    bundle: bundle/0.1.0
    routes:
      default:
        entry: index.html
        path:
          - /
