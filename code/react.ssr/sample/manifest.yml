sites:
  - name: dev
    domain:
      - localhost
      - /.*\.now\.sh$/
    bundle: bundle/0.9.2
    routes:
      default:
        entry: index.html
        path:
          - /
          - /foo
  - name: prod
    domain:
      - ssr.platform.uiharness.com
    bundle: bundle/0.9.1
    routes:
      default:
        entry: index.html
        path:
          - /
