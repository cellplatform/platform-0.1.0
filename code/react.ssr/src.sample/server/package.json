{
  "name": "sample.ssr.server",
  "version": "0.0.22",
  "scripts": {
    "ssr": "ssr $@",
    "now": "now dev --listen 8080",
    "deploy": "now --prod",
    "start": "ts-node -T src/server",
    "watch": "nodemon --watch ../../src -e ts,tsx --delay 2"
  },
  "dependencies": {
    "@platform/react.ssr": "^1.1.11"
  },
  "devDependencies": {
    "ts-node": "^8.3.0"
  },
  "engines": {
    "node": ">=10.x"
  },
  "license": "MIT",
  "private": true
}
