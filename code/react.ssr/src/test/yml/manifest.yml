# 
# Sample manifest for `@platform/react.ssr`
# 

sites:
  - domain: [localhost, /.*\.now\.sh$/]
    bundle: bundle/1.2.3-alpha.0
    routes:
      default:
        entry: index.html
        path: ["/", "/foo"]

