# 
# Sample manifest for `@platform/react.ssr`
# 

sites:
  - domain: [localhost, /.*\.now\.sh$/]
    bundle: bundle/0.4.12-alpha.0
    routes:
      default:
        entry: index.html
        path: ["/", "/foo"]

