sites:
  - name: dev
    domain:
      - localhost
      - /.*\.now\.sh$/
    bundle: bundle/0.7.0
    routes:
      default:
        entry: index.html
        path:
          - /
          - /foo
  - name: prod
    domain:
      - platform.uiharness.com
      - /.*\.now\.sh$/
    bundle: bundle/0.9.2
    routes:
      default:
        entry: index.html
        path:
          - /
