# 
# Sample manifest for `@platform/react.ssr`
# 

sites:
  - name: dev
    domain: [localhost, /.*\.now\.sh$/]
    bundle: bundle/0.5.17-beta.0
    routes:
      default:
        entry: index.html
        path: ["/", "/foo"]

  - name: prod
    domain: [platform.uiharness.com, /.*\.now\.sh$/]
    bundle: bundle/0.5.17-beta.0
    routes:
      default:
        entry: index.html
        path: ["/", "/foo"]

