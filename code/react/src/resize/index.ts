import { useResizeObserver } from '../hooks/useResizeObserver';
import { ResizeObserver as observer } from './ResizeObserver';

export { useResizeObserver };
export const Resize = { useResizeObserver, observer };
