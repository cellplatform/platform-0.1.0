export { is } from '@platform/util.is';
export { GlamorValue, css, color } from '@platform/css';
export { log } from '@platform/log/lib/client/log';

/**
 * Ramda
 */
import { equals, uniq } from 'ramda';
export const R = { equals, uniq };
