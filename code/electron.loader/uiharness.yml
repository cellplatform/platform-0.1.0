#
# https://uiharness.com/docs/config
#

name: electron.loader

electron:
  port: 3427
  entry:
    main: test/main.ts
    renderer: 
      default: test/renderer.tsx
      sample: 
        path: test/renderer/two.tsx # Example secondary entry. Point this to another entry file.
        title: Two (v${version})


web:
  port: 1234
  entry: test/web.tsx

sourcemaps:
  strip:
    - node_modules/rxjs

