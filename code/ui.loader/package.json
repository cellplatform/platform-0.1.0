{
  "name": "@platform/ui.loader",
  "version": "0.0.1-beta.0",
  "description": "Module bootstrapper for progressively loading UI modules dynamically.",
  "main": "lib/index",
  "types": "lib/index.d.ts",
  "scripts": {
    "ssr": "cd sample.server && yarn ssr $@",
    "ui": "ui $@",
    "start": "ui start web $@",
    "watch": "cd sample.server && yarn watch $@",
    "test": "ts test",
    "tdd": "ts test --watch",
    "lint": "ts lint",
    "build": "ts build $@",
    "build-test": "ts build --tsconfig=tsconfig.test $@",
    "prepare": "ts prepare",
    "bundle": "ui bundle web"
  },
  "dependencies": {
    "@platform/react": "0.3.20"
  },
  "devDependencies": {
    "@uiharness/dev": "0.9.3",
    "@uiharness/ui": "0.6.14"
  },
  "files": [
    "lib",
    "!lib/test",
    "!*.TEST.{js, mjs, d.ts}"
  ],
  "license": "MIT",
  "private": true
}
