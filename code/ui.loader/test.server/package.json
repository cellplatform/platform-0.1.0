{
  "name": "sample.loader.server",
  "version": "0.0.5",
  "scripts": {
    "ssr": "ssr $@",
    "now": "now dev --listen 8080",
    "deploy": "now --prod",
    "start": "ts-node -T src/server",
    "watch": "nodemon --watch ../../src -e ts,tsx --delay 2"
  },
  "dependencies": {
    "@platform/react.ssr": "^1.0.5"
  },
  "devDependencies": {
    "ts-node": "^8.3.0"
  },
  "license": "MIT",
  "private": true
}
