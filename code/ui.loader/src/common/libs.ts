import '@platform/libs/polyfill';

export { css, color, GlamorValue, is } from '@platform/react';
export { time, defaultValue, id } from '@platform/util.value';
export { log } from '@platform/log/lib/client';
